{
  "name": "lero-lero-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start:disc": "node build/apps/discord/main.js",
    "run:disc": "NODE_ENV=PROD pnpm build && pnpm start:disc",
    "deploy:disc": "pnpm build && NODE_ENV=PROD_REMOTE pnpm start:disc",
    "dev:disc": "NODE_ENV=DEV ts-node-dev --respawn --transpile-only apps/discord/main.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.478.0",
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.1",
    "openai": "^4.22.1"
  }
}
